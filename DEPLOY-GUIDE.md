# راهنمای کامل راه‌اندازی سایت کلینیک المپیک (واقعی و آنلاین)

این پکیج شامل یک سایت کامل + پنل مدیریت است. در ادامه، مرحله‌به‌مرحله توضیح داده شده که چطور آن را از حالت فایل به یک سایت واقعی روی اینترنت تبدیل کنید.

## فایل‌های این پکیج چه هستند؟

```
olympic-clinic-site/
├── public/index.html     ← خود سایت (چیزی که بازدیدکننده می‌بیند)
├── admin/index.html      ← پنل مدیریت (رابط ورود و ویرایش)
├── data/content.json     ← تمام متن‌های سایت اینجا ذخیره می‌شوند
├── uploads/               ← عکس‌هایی که از پنل آپلود می‌کنید اینجا می‌رود
├── server.js              ← برنامه سرور (Node.js) که همه چیز را به هم وصل می‌کند
├── package.json           ← لیست ابزارهایی که سرور به آن نیاز دارد
├── .env.example            ← نمونه فایل تنظیمات امنیتی (رمز عبور و...)
└── .gitignore
```

**پنل مدیریت چطور کار می‌کند؟** شما وارد آدرس `سایت‌شما.com/admin` می‌شوید، با نام کاربری و رمزی که خودتان تعیین می‌کنید وارد می‌شوید، متن‌ها (تلفن، آدرس، ساعات کاری، توضیح خدمات، نظر مراجعه‌کننده و ...) را عوض می‌کنید و دکمه «ذخیره تغییرات» را می‌زنید. تغییرات فوراً روی سایت اصلی اعمال می‌شود؛ نیازی به دست‌کاری کد نیست.

---

## مرحله ۱ — خرید دامنه (آدرس سایت)

1. یک دامنه بخرید، مثلاً `olympicclinic.ir` یا `olympicclinic.com`.
2. جاهای معتبر برای خرید دامنه ایرانی: **ایران‌هاست، پارس‌پک، آبان‌هاست** (برای `.ir`) یا **Namecheap / Cloudflare Registrar** (برای `.com`).
3. قیمت معمولاً سالانه بین ۱۵۰ تا ۵۰۰ هزار تومان است بسته به پسوند دامنه.

## مرحله ۲ — انتخاب هاست (جایی که سرور اجرا می‌شود)

چون این سایت یک برنامه Node.js دارد (نه فقط HTML ساده)، باید هاستی انتخاب کنید که Node.js را اجرا کند. دو مسیر پیشنهادی:

### گزینه الف) ساده‌ترین راه — هاست‌های ایرانی مخصوص Node.js (پیشنهاد می‌شود)
- **لیارا (Liara.ir)** یا **آروان کلود (ArvanCloud)**
- مزیت: پنل فارسی، پرداخت ریالی، پشتیبانی فارسی، دیپلوی با چند کلیک
- مراحل کلی: ثبت‌نام → ساخت «اپلیکیشن Node.js» جدید → آپلود پروژه (از طریق Git یا آپلود مستقیم فایل zip) → تنظیم متغیرهای محیطی (`.env`) → دیپلوی

### گزینه ب) سرور مجازی (VPS) — کنترل کامل‌تر، کمی فنی‌تر
- ارائه‌دهنده‌های VPS: **آبرآل (Ab-r)، پارس‌پک، Hetzner، DigitalOcean**
- این روش قدرت بیشتری می‌دهد ولی نیاز به کار با ترمینال دارد (مراحل کامل در پایین همین راهنما آمده).

اگر می‌خواهید ساده‌ترین مسیر را بروید، **لیارا** را انتخاب کنید.

---

## مرحله ۳ — راه‌اندازی روی لیارا (Liara) — گام‌به‌گام

1. به [liara.ir](https://liara.ir) بروید و ثبت‌نام کنید.
2. از منو، «اپلیکیشن‌ها» → «ساخت اپلیکیشن جدید» را بزنید.
3. نوع پلتفرم را **Node.js** انتخاب کنید.
4. یک نام برای اپ انتخاب کنید (مثلاً `olympic-clinic`).
5. پروژه را آپلود کنید. دو راه دارید:
   - **آپلود مستقیم:** کل پوشه `olympic-clinic-site` را zip کنید و در پنل لیارا آپلود کنید.
   - **از طریق Git (پیشنهادی):** پروژه را در یک ریپازیتوری GitHub خصوصی قرار دهید و لیارا را به آن وصل کنید تا با هر تغییر، خودش دوباره دیپلوی کند.
6. در بخش «متغیرهای محیطی» (Environment Variables) این مقادیر را وارد کنید (دقیقاً از فایل `.env.example`):
   - `ADMIN_USER` → نام کاربری دلخواه شما برای ورود به پنل
   - `ADMIN_PASS` → یک رمز عبور قوی (حتماً عوض کنید، رمز پیش‌فرض را نگه ندارید)
   - `SESSION_SECRET` → یک رشته تصادفی طولانی (مثلاً از سایت [random.org](https://www.random.org/strings/) بگیرید)
   - `PORT` → لیارا خودش این را مدیریت می‌کند، لازم نیست دستی بدهید
7. دکمه دیپلوی را بزنید. بعد از چند دقیقه، لیارا یک آدرس موقت به شما می‌دهد (مثل `olympic-clinic.liara.run`).
8. از بخش «دامنه‌ها» (Domains) در پنل اپ، دامنه‌ای که در مرحله ۱ خریدید را اضافه کنید. لیارا رکوردهای DNS لازم (معمولاً یک رکورد از نوع A یا CNAME) را نشان می‌دهد.
9. وارد پنل مدیریت دامنه‌تان (همان جایی که دامنه را خریدید) شوید و آن رکورد DNS را اضافه کنید.
10. معمولاً بین ۱۰ دقیقه تا چند ساعت طول می‌کشد تا دامنه فعال شود. لیارا خودش SSL رایگان (قفل سبز https) را فعال می‌کند.

بعد از این مراحل، سایت شما روی `https://olympicclinic.com` و پنل مدیریت روی `https://olympicclinic.com/admin` در دسترس است.

---

## مسیر جایگزین — راه‌اندازی روی یک VPS (سرور مجازی) با دامنه و SSL

اگر ترجیح می‌دهید روی سرور اختصاصی خودتان اجرا کنید:

### ۱. خرید VPS
یک VPS با حداقل ۱ گیگابایت رم بخرید (اوبونتو ۲۲.۰۴ پیشنهاد می‌شود).

### ۲. اتصال به سرور
از کامپیوترتان با ترمینال (یا PuTTY در ویندوز) وصل شوید:
```bash
ssh root@IP-سرور-شما
```

### ۳. نصب Node.js
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
node -v
```

### ۴. آپلود پروژه
پوشه `olympic-clinic-site` را با یکی از این روش‌ها به سرور منتقل کنید:
```bash
# از کامپیوتر خودتان اجرا کنید (نه داخل سرور):
scp -r olympic-clinic-site root@IP-سرور-شما:/var/www/
```

### ۵. نصب پکیج‌ها و تنظیم .env
```bash
cd /var/www/olympic-clinic-site
npm install
cp .env.example .env
nano .env      # مقادیر ADMIN_USER, ADMIN_PASS, SESSION_SECRET را عوض کنید و ذخیره کنید (Ctrl+O سپس Ctrl+X)
```

### ۶. اجرای دائمی سرور با PM2
اگر سرور را مستقیم با `node server.js` اجرا کنید، با بسته شدن ترمینال، سایت هم خاموش می‌شود. برای اجرای همیشگی از PM2 استفاده کنید:
```bash
npm install -g pm2
pm2 start server.js --name olympic-clinic
pm2 save
pm2 startup     # دستوری که نشان می‌دهد را کپی و اجرا کنید تا با ری‌استارت سرور، سایت خودکار بالا بیاید
```

### ۷. نصب Nginx (برای وصل کردن دامنه به پورت سرور)
```bash
apt-get install -y nginx
nano /etc/nginx/sites-available/olympicclinic
```
این محتوا را در فایل قرار دهید:
```nginx
server {
    listen 80;
    server_name olympicclinic.com www.olympicclinic.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```
سپس:
```bash
ln -s /etc/nginx/sites-available/olympicclinic /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

### ۸. تنظیم DNS دامنه
در پنل مدیریت دامنه‌تان، یک رکورد از نوع **A** بسازید که به IP سرورتان اشاره کند:
```
Type: A
Name: @
Value: IP-سرور-شما
```
و یک رکورد مشابه برای `www` هم بسازید.

### ۹. فعال‌سازی SSL رایگان (قفل https)
```bash
apt-get install -y certbot python3-certbot-nginx
certbot --nginx -d olympicclinic.com -d www.olympicclinic.com
```
از سوالاتی که می‌پرسد (ایمیل، قبول قوانین) عبور کنید؛ Certbot خودش گواهی SSL را نصب و تنظیم می‌کند.

حالا سایت شما روی `https://olympicclinic.com` بالا است.

---

## مرحله بعد از راه‌اندازی — استفاده روزانه از پنل مدیریت

1. به آدرس `سایت‌شما.com/admin` بروید.
2. با نام کاربری و رمزی که در `.env` تعیین کردید وارد شوید.
3. هر بخشی از سایت (تلفن، آدرس، ساعات کاری، متن هر خدمت، نظر مراجعه‌کننده، لینک اینستاگرام و ...) را ویرایش کنید.
4. دکمه **«ذخیره تغییرات روی سایت»** را بزنید.
5. تغییرات همان لحظه روی سایت اصلی نمایش داده می‌شود — کافی است صفحه سایت را رفرش کنید.

## نکات امنیتی مهم
- حتماً `ADMIN_PASS` را به یک رمز قوی و غیرقابل حدس تغییر دهید (نه چیزی مثل `1234`).
- فایل `.env` را هرگز در جایی عمومی (مثل یک ریپازیتوری Git عمومی) قرار ندهید؛ فایل `.gitignore` همراه پروژه این را به‌طور خودکار رعایت می‌کند.
- هر چند وقت یک‌بار از فایل `data/content.json` یک نسخه پشتیبان بگیرید (کپی کنید و جایی نگه دارید)، تا در صورت بروز خطا بتوانید برگردانید.

## اگر جایی گیر کردید
اگر در هر کدام از این مراحل (خرید دامنه، تنظیم DNS، نصب روی لیارا یا VPS) به مشکل خوردید، پیام خطا یا اسکرین‌شات را برایم بفرستید تا دقیقاً همان مرحله را با هم حل کنیم.
